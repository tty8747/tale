version: 2
jobs:
  build:
    working_directory: ~/tale

    docker: 
      - image: circleci/openjdk:8-jdk-browsers 

    steps: 

      - checkout
      - run:
          name: HI!
          command: echo "Hi, World!"
      - run:
          name: run pwd
          command: pwd
      - run:
          name: mvn
          command: which mvn
